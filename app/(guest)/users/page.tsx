import { UserListPageContainer } from '@/features/user/components/server/user-list-page-container';

export default function UserListPage(_props: PageProps<'/users'>) {
  return <UserListPageContainer />;
}
